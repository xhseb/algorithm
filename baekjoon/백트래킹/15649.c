#include <stdio.h>

void    func(int max, int i, int dep, int j)
{
    if ()
}

int     main(void)
{
    int     max, dep;
    int     arr[8] = {0, };

    scanf("%d %d", &max, &dep);
    func(max, 1, dep, 0);
}